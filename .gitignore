# Secrets — never commit
.env

# Python
.venv/
__pycache__/
*.pyc

# Local cache from prelabel.py (not needed in the repo)
prelabel_cache.json

# Optional: uncomment if you don't want the raw saved JSON in the repo
# raw_json/

# Runtime data (regenerated each run; curated sample lives in README)
audit_log.jsonl
submissions.json
